script: 
  - "gcc src/hello.c -o hello"
  - "ls -l"
  - "file hello"
  - "./hello"
  - "./build-bootstrap.sh"
deploy:
  provider: releases
  file: "hello"
  skip_cleanup: true
  api_key: $auth
  on:
    tags: true  
